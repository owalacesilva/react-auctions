// import { selectAuctionListPageDomain } from '../selectors';

describe('selectAuctionListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
