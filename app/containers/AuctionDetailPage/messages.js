/*
 * AuctionDetailPage Messages
 *
 * This contains all the text for the AuctionDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuctionDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuctionDetailPage container!',
  },
});
