// import { selectAuctionDetailPageDomain } from '../selectors';

describe('selectAuctionDetailPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
