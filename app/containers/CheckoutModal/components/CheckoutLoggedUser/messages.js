/*
 * CheckoutLoggedUser Messages
 *
 * This contains all the text for the CheckoutLoggedUser component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CheckoutLoggedUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CheckoutLoggedUser component!',
  },
});
