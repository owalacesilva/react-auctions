/*
 * MenuModal Messages
 *
 * This contains all the text for the MenuModal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MenuModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuModal container!',
  },
});
