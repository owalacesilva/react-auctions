/*
 * DateTimeFormat Messages
 *
 * This contains all the text for the DateTimeFormat component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DateTimeFormat';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DateTimeFormat component!',
  },
});
