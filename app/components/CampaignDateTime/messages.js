/*
 * CampaignDateTime Messages
 *
 * This contains all the text for the CampaignDateTime component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CampaignDateTime';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CampaignDateTime component!',
  },
});
