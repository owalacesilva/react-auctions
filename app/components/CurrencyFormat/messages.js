/*
 * CurrencyFormat Messages
 *
 * This contains all the text for the CurrencyFormat component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CurrencyFormat';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CurrencyFormat component!',
  },
});
