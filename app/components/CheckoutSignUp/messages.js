/*
 * CheckoutSignUp Messages
 *
 * This contains all the text for the CheckoutSignUp component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CheckoutSignUp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CheckoutSignUp component!',
  },
});
